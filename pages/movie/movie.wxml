<view class="container">
  <scroll-view class="movies" scroll-y="true" bindscrolltolower="getMovies">
    <view wx:for="{{movies}}" class="movie" wx:key="id">
      <image class="movie-cover" src="{{item.cover}}" mode="scaleToFill"></image>
    </view>
  </scroll-view>
</view>
